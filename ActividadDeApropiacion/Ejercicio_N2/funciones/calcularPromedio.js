// Se hace la funcion para recibir las 3 notas

export function calcularPromedio (nota1, nota2, nota3){
    // Variable para poder guardar el promedio
    let promedio = (nota1,nota2,nota3) / 3

    // retorna el promdio
    return promedio
}